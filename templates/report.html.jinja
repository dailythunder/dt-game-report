
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Daily Thunder Game Report - {{ data.meta.game_id }}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    :root {
      --okc-blue: #007ac1;
      --okc-orange: #f05133;
      --bg: #f5f5f5;
      --card-bg: #ffffff;
      --text-main: #222222;
      --text-muted: #666666;
      --border-subtle: #dddddd;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background-color: var(--bg);
      color: var(--text-main);
    }

    .page {
      max-width: 1200px;
      margin: 0 auto;
      padding: 24px 16px 40px;
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 16px;
      gap: 16px;
    }

    .header-left {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }

    .brand {
      font-size: 14px;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--text-muted);
    }

    .title {
      font-size: 26px;
      font-weight: 700;
      color: var(--okc-blue);
    }

    .subtitle {
      font-size: 14px;
      color: var(--text-muted);
    }

    .scoreline {
      text-align: right;
    }

    .scoreline-main {
      font-size: 22px;
      font-weight: 700;
    }

    .scoreline-teams {
      font-size: 14px;
      color: var(--text-muted);
    }

    .pill-row {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin-bottom: 16px;
    }

    .pill {
      padding: 4px 10px;
      border-radius: 999px;
      background-color: #e1f0fa;
      color: var(--okc-blue);
      font-size: 12px;
      font-weight: 500;
    }

    .layout {
      display: flex;
      flex-direction: column;
      gap: 16px;
    }

    .card {
      background-color: var(--card-bg);
      border-radius: 12px;
      padding: 16px;
      box-shadow: 0 2px 6px rgba(0, 0, 0, 0.03);
      border: 1px solid var(--border-subtle);
    }

    .card h2 {
      font-size: 18px;
      margin: 0 0 8px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .card h3 {
      font-size: 16px;
      margin: 16px 0 8px;
    }

    .section-label {
      font-size: 13px;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.08em;
      margin-bottom: 4px;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 13px;
    }

    th, td {
      padding: 4px 6px;
      text-align: right;
      border-bottom: 1px solid #eeeeee;
      white-space: nowrap;
    }

    th:first-child,
    td:first-child {
      text-align: left;
    }

    thead tr {
      background-color: #f0f4f8;
    }

    tbody tr:nth-child(even) {
      background-color: #fafafa;
    }

    .team-name-cell {
      font-weight: 600;
    }

    .totals-row {
      font-weight: 600;
      background-color: #eef5ff;
    }

    .leaders-chip {
      display: inline-flex;
      padding: 2px 8px;
      border-radius: 999px;
      background-color: #f8f9ff;
      border: 1px solid #e0e3ff;
      font-size: 12px;
      margin-right: 6px;
      margin-bottom: 4px;
    }

    .leaders-chip span.label {
      font-weight: 600;
      margin-right: 4px;
    }

    .quarters-wrapper {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .quarter-block {
      border-top: 1px solid var(--border-subtle);
      padding-top: 12px;
      margin-top: 4px;
    }

    .quarter-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: 4px;
      gap: 8px;
    }

    .quarter-header-left {
      font-weight: 600;
      font-size: 14px;
    }

    .quarter-header-right {
      font-size: 12px;
      color: var(--text-muted);
    }

    .quarter-card {
      margin-top: 4px;
    }

    .quarter-team-label {
      font-weight: 600;
      font-size: 13px;
      margin-top: 6px;
      margin-bottom: 4px;
    }

    .score-by-quarter-table th,
    .score-by-quarter-table td {
      text-align: center;
    }

    .score-by-quarter-table th:first-child,
    .score-by-quarter-table td:first-child {
      text-align: left;
    }

    @media (max-width: 768px) {
      .header {
        flex-direction: column;
        align-items: flex-start;
      }

      .scoreline {
        text-align: left;
      }

      th, td {
        font-size: 12px;
      }
    }
  </style>
</head>
<body>
  <div class="page">
    <header class="header">
      <div class="header-left">
        <div class="brand">Daily Thunder Game Report</div>
        <div class="title">
          {{ data.teams.home.tricode }} vs {{ data.teams.away.tricode }}
        </div>
        <div class="subtitle">
          {{ data.meta.date }} &middot; Game ID {{ data.meta.game_id }}
        </div>
      </div>
      <div class="scoreline">
        <div class="scoreline-main">
          {{ data.teams.home.tricode }} {{ data.meta.final_score_home }}
          &nbsp;–&nbsp;
          {{ data.meta.final_score_away }} {{ data.teams.away.tricode }}
        </div>
      </div>
    </header>

    <div class="pill-row">
      <div class="pill">{{ data.teams.home.full_name }}</div>
      <div class="pill">{{ data.teams.away.full_name }}</div>
      <div class="pill">Season {{ data.meta.season }}</div>
    </div>

    <main class="layout">
      <!-- Score by quarter + largest lead -->
      <section class="card">
        <div class="section-label">Score Summary</div>
        <h2>
          Score by Quarter
          <span style="font-size: 13px; font-weight: 400; color: var(--text-muted);">
            Largest lead: {{ data.largest_lead.home }} ({{ data.teams.home.tricode }}) /
            {{ data.largest_lead.away }} ({{ data.teams.away.tricode }})
          </span>
        </h2>
        <table class="score-by-quarter-table">
          <thead>
            <tr>
              <th>Team</th>
              {% for q in data.quarters %}
                <th>Q{{ q.number }}</th>
              {% endfor %}
              <th>Total</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td class="team-name-cell">{{ data.teams.home.tricode }}</td>
              {% set home_total = 0 %}
              {% for q in data.quarters %}
                <td>
                  {{ q.home_score }}
                  {% set home_total = home_total + (q.home_score or 0) %}
                </td>
              {% endfor %}
              <td>{{ home_total }}</td>
            </tr>
            <tr>
              <td class="team-name-cell">{{ data.teams.away.tricode }}</td>
              {% set away_total = 0 %}
              {% for q in data.quarters %}
                <td>
                  {{ q.away_score }}
                  {% set away_total = away_total + (q.away_score or 0) %}
                </td>
              {% endfor %}
              <td>{{ away_total }}</td>
            </tr>
          </tbody>
        </table>
      </section>

      <!-- Team traditional totals -->
      <section class="card">
        <div class="section-label">Team Stats</div>
        <h2>Full Game – Traditional Team Totals</h2>
        <table>
          <thead>
            <tr>
              <th>Team</th>
              <th>FG</th>
              <th>3P</th>
              <th>FT</th>
              <th>REB</th>
              <th>AST</th>
              <th>STL</th>
              <th>BLK</th>
              <th>TOV</th>
              <th>PF</th>
              <th>PTS</th>
              <th>FB Pts</th>
            </tr>
          </thead>
          <tbody>
            {% set home_trad = data.game_totals.traditional.home %}
            {% set away_trad = data.game_totals.traditional.away %}
            {% set misc = data.game_totals.misc %}
            {% set misc_home = misc.home if misc is defined else {} %}
            {% set misc_away = misc.away if misc is defined else {} %}

            <tr>
              <td class="team-name-cell">{{ data.teams.home.tricode }}</td>
              <td>{{ home_trad.fg }}/{{ home_trad.fga }}</td>
              <td>{{ home_trad.fg3 }}/{{ home_trad.fg3a }}</td>
              <td>{{ home_trad.ft }}/{{ home_trad.fta }}</td>
              <td>{{ home_trad.trb }}</td>
              <td>{{ home_trad.ast }}</td>
              <td>{{ home_trad.stl }}</td>
              <td>{{ home_trad.blk }}</td>
              <td>{{ home_trad.tov }}</td>
              <td>{{ home_trad.pf }}</td>
              <td>{{ home_trad.pts }}</td>
              <td>{{ misc_home.fast_break if misc_home else 0 }}</td>
            </tr>
            <tr>
              <td class="team-name-cell">{{ data.teams.away.tricode }}</td>
              <td>{{ away_trad.fg }}/{{ away_trad.fga }}</td>
              <td>{{ away_trad.fg3 }}/{{ away_trad.fg3a }}</td>
              <td>{{ away_trad.ft }}/{{ away_trad.fta }}</td>
              <td>{{ away_trad.trb }}</td>
              <td>{{ away_trad.ast }}</td>
              <td>{{ away_trad.stl }}</td>
              <td>{{ away_trad.blk }}</td>
              <td>{{ away_trad.tov }}</td>
              <td>{{ away_trad.pf }}</td>
              <td>{{ away_trad.pts }}</td>
              <td>{{ misc_away.fast_break if misc_away else 0 }}</td>
            </tr>
          </tbody>
        </table>
      </section>

      <!-- Leaders -->
      <section class="card">
        <div class="section-label">Leaders</div>
        <h2>Team Leaders – Traditional</h2>
        <div>
          <h3>{{ data.teams.home.full_name }}</h3>
          {% set home_leaders = data.leaders.home %}
          <div>
            <div class="leaders-chip">
              <span class="label">PTS</span>
              {{ home_leaders.points.value }} – {{ home_leaders.points.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">REB</span>
              {{ home_leaders.rebounds.value }} – {{ home_leaders.rebounds.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">AST</span>
              {{ home_leaders.assists.value }} – {{ home_leaders.assists.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">STL</span>
              {{ home_leaders.steals.value }} – {{ home_leaders.steals.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">BLK</span>
              {{ home_leaders.blocks.value }} – {{ home_leaders.blocks.players | join(", ") }}
            </div>
          </div>
        </div>
        <div style="margin-top: 8px;">
          <h3>{{ data.teams.away.full_name }}</h3>
          {% set away_leaders = data.leaders.away %}
          <div>
            <div class="leaders-chip">
              <span class="label">PTS</span>
              {{ away_leaders.points.value }} – {{ away_leaders.points.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">REB</span>
              {{ away_leaders.rebounds.value }} – {{ away_leaders.rebounds.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">AST</span>
              {{ away_leaders.assists.value }} – {{ away_leaders.assists.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">STL</span>
              {{ away_leaders.steals.value }} – {{ away_leaders.steals.players | join(", ") }}
            </div>
            <div class="leaders-chip">
              <span class="label">BLK</span>
              {{ away_leaders.blocks.value }} – {{ away_leaders.blocks.players | join(", ") }}
            </div>
          </div>
        </div>
      </section>

      <!-- Full game player box scores (traditional only) -->
      <section class="card">
        <div class="section-label">Full Game Box Score</div>
        <h2>Traditional – Players</h2>

        {% for side in ["home", "away"] %}
          {% set team = data.teams[side] %}
          {% set players = data.players[side] %}
          <h3>{{ team.full_name }} ({{ team.tricode }})</h3>
          <table>
            <thead>
              <tr>
                <th>Player</th>
                <th>MIN</th>
                <th>FG</th>
                <th>3P</th>
                <th>FT</th>
                <th>REB</th>
                <th>AST</th>
                <th>STL</th>
                <th>BLK</th>
                <th>TOV</th>
                <th>PF</th>
                <th>PTS</th>
              </tr>
            </thead>
            <tbody>
              {% set team_totals = {
                "min": "",
                "fg": 0,
                "fga": 0,
                "fg3": 0,
                "fg3a": 0,
                "ft": 0,
                "fta": 0,
                "trb": 0,
                "ast": 0,
                "stl": 0,
                "blk": 0,
                "tov": 0,
                "pf": 0,
                "pts": 0
              } %}

              {% for p in players %}
                {% set stats = p.traditional if p.traditional is defined and p.traditional is mapping else p %}
                <tr>
                  <td class="team-name-cell">{{ p.name }}</td>
                  <td>{{ p.min }}</td>
                  <td>{{ stats.fg }}/{{ stats.fga }}</td>
                  <td>{{ stats.fg3 }}/{{ stats.fg3a }}</td>
                  <td>{{ stats.ft }}/{{ stats.fta }}</td>
                  <td>{{ stats.trb }}</td>
                  <td>{{ stats.ast }}</td>
                  <td>{{ stats.stl }}</td>
                  <td>{{ stats.blk }}</td>
                  <td>{{ stats.tov }}</td>
                  <td>{{ stats.pf }}</td>
                  <td>{{ stats.pts }}</td>
                </tr>
                {% set _ = team_totals.update({
                  "fg": team_totals.fg + (stats.fg or 0),
                  "fga": team_totals.fga + (stats.fga or 0),
                  "fg3": team_totals.fg3 + (stats.fg3 or 0),
                  "fg3a": team_totals.fg3a + (stats.fg3a or 0),
                  "ft": team_totals.ft + (stats.ft or 0),
                  "fta": team_totals.fta + (stats.fta or 0),
                  "trb": team_totals.trb + (stats.trb or 0),
                  "ast": team_totals.ast + (stats.ast or 0),
                  "stl": team_totals.stl + (stats.stl or 0),
                  "blk": team_totals.blk + (stats.blk or 0),
                  "tov": team_totals.tov + (stats.tov or 0),
                  "pf": team_totals.pf + (stats.pf or 0),
                  "pts": team_totals.pts + (stats.pts or 0)
                }) %}
              {% endfor %}

              <tr class="totals-row">
                <td class="team-name-cell">Team Totals</td>
                <td></td>
                <td>{{ team_totals.fg }}/{{ team_totals.fga }}</td>
                <td>{{ team_totals.fg3 }}/{{ team_totals.fg3a }}</td>
                <td>{{ team_totals.ft }}/{{ team_totals.fta }}</td>
                <td>{{ team_totals.trb }}</td>
                <td>{{ team_totals.ast }}</td>
                <td>{{ team_totals.stl }}</td>
                <td>{{ team_totals.blk }}</td>
                <td>{{ team_totals.tov }}</td>
                <td>{{ team_totals.pf }}</td>
                <td>{{ team_totals.pts }}</td>
              </tr>
            </tbody>
          </table>
        {% endfor %}
      </section>

      <!-- Quarter-by-quarter traditional (team + players) -->
      <section class="card">
        <div class="section-label">Quarter by Quarter</div>
        <h2>Traditional – Team & Players</h2>
        <div class="quarters-wrapper">
          {% for q in data.quarters %}
            <div class="quarter-block">
              <div class="quarter-header">
                <div class="quarter-header-left">Quarter {{ q.number }}</div>
                <div class="quarter-header-right">
                  Score: {{ data.teams.home.tricode }} {{ q.home_score }} – {{ q.away_score }} {{ data.teams.away.tricode }}
                </div>
              </div>

              {% set q_team_trad = q.team_totals.traditional %}

              <div class="quarter-card">
                <div class="quarter-team-label">{{ data.teams.home.full_name }} ({{ data.teams.home.tricode }}) – Team Totals</div>
                {% set h = q_team_trad.home %}
                <table>
                  <thead>
                    <tr>
                      <th>Team</th>
                      <th>FG</th>
                      <th>3P</th>
                      <th>FT</th>
                      <th>REB</th>
                      <th>AST</th>
                      <th>STL</th>
                      <th>BLK</th>
                      <th>TOV</th>
                      <th>PTS</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr>
                      <td class="team-name-cell">{{ data.teams.home.tricode }}</td>
                      <td>{{ h.fg }}/{{ h.fga }}</td>
                      <td>{{ h.fg3 }}/{{ h.fg3a }}</td>
                      <td>{{ h.ft }}/{{ h.fta }}</td>
                      <td>{{ h.trb }}</td>
                      <td>{{ h.ast }}</td>
                      <td>{{ h.stl }}</td>
                      <td>{{ h.blk }}</td>
                      <td>{{ h.tov }}</td>
                      <td>{{ h.pts }}</td>
                    </tr>
                  </tbody>
                </table>
              </div>

              <div class="quarter-card">
                <div class="quarter-team-label">{{ data.teams.away.full_name }} ({{ data.teams.away.tricode }}) – Team Totals</div>
                {% set a = q_team_trad.away %}
                <table>
                  <thead>
                    <tr>
                      <th>Team</th>
                      <th>FG</th>
                      <th>3P</th>
                      <th>FT</th>
                      <th>REB</th>
                      <th>AST</th>
                      <th>STL</th>
                      <th>BLK</th>
                      <th>TOV</th>
                      <th>PTS</th>
                    </tr>
                  </thead>
                  <tbody>
                    <tr>
                      <td class="team-name-cell">{{ data.teams.away.tricode }}</td>
                      <td>{{ a.fg }}/{{ a.fga }}</td>
                      <td>{{ a.fg3 }}/{{ a.fg3a }}</td>
                      <td>{{ a.ft }}/{{ a.fta }}</td>
                      <td>{{ a.trb }}</td>
                      <td>{{ a.ast }}</td>
                      <td>{{ a.stl }}</td>
                      <td>{{ a.blk }}</td>
                      <td>{{ a.tov }}</td>
                      <td>{{ a.pts }}</td>
                    </tr>
                  </tbody>
                </table>
              </div>

              <!-- Player stats for the quarter -->
              <div class="quarter-card">
                <div class="quarter-team-label">{{ data.teams.home.full_name }} ({{ data.teams.home.tricode }}) – Players</div>
                {% set q_home_players = q.players.home %}
                <table>
                  <thead>
                    <tr>
                      <th>Player</th>
                      <th>MIN</th>
                      <th>FG</th>
                      <th>3P</th>
                      <th>FT</th>
                      <th>REB</th>
                      <th>AST</th>
                      <th>STL</th>
                      <th>BLK</th>
                      <th>TOV</th>
                      <th>PTS</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% for p in q_home_players %}
                      <tr>
                        <td class="team-name-cell">{{ p.name }}</td>
                        <td>{{ p.min }}</td>
                        <td>{{ p.fg }}/{{ p.fga }}</td>
                        <td>{{ p.fg3 }}/{{ p.fg3a }}</td>
                        <td>{{ p.ft }}/{{ p.fta }}</td>
                        <td>{{ p.trb }}</td>
                        <td>{{ p.ast }}</td>
                        <td>{{ p.stl }}</td>
                        <td>{{ p.blk }}</td>
                        <td>{{ p.tov }}</td>
                        <td>{{ p.pts }}</td>
                      </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>

              <div class="quarter-card">
                <div class="quarter-team-label">{{ data.teams.away.full_name }} ({{ data.teams.away.tricode }}) – Players</div>
                {% set q_away_players = q.players.away %}
                <table>
                  <thead>
                    <tr>
                      <th>Player</th>
                      <th>MIN</th>
                      <th>FG</th>
                      <th>3P</th>
                      <th>FT</th>
                      <th>REB</th>
                      <th>AST</th>
                      <th>STL</th>
                      <th>BLK</th>
                      <th>TOV</th>
                      <th>PTS</th>
                    </tr>
                  </thead>
                  <tbody>
                    {% for p in q_away_players %}
                      <tr>
                        <td class="team-name-cell">{{ p.name }}</td>
                        <td>{{ p.min }}</td>
                        <td>{{ p.fg }}/{{ p.fga }}</td>
                        <td>{{ p.fg3 }}/{{ p.fg3a }}</td>
                        <td>{{ p.ft }}/{{ p.fta }}</td>
                        <td>{{ p.trb }}</td>
                        <td>{{ p.ast }}</td>
                        <td>{{ p.stl }}</td>
                        <td>{{ p.blk }}</td>
                        <td>{{ p.tov }}</td>
                        <td>{{ p.pts }}</td>
                      </tr>
                    {% endfor %}
                  </tbody>
                </table>
              </div>
            </div>
          {% endfor %}
        </div>
      </section>
    </main>
  </div>
</body>
</html>
